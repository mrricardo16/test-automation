# ID Contract

新建 TestCase 使用：

```text
TC-<DOMAIN>-<CASE_NAME>
```

例如：`TC-SYN-API-CRUD-001`、`TC-WEB-LOGIN-001`、`TC-AVA-ANALYSIS-001`。

历史 `TC-*` ID 不重命名、不复用、不覆盖。`INFRASTRUCTURE_SELF_TEST` 作为基础设施自测保留值。

脚本、结果、Coverage、Defect 和 Evidence 必须携带同一个 `TestCaseId`。
