# Automation scaffold

`web/`, `api/`, and `helpers/` are scaffolds only. No formal regression test is registered or executable while the execution gate is blocked. Historic diagnostics are retained only as `CANDIDATE_FOR_MIGRATION`, never promoted to a formal regression without an approved handoff-integrity gate.
