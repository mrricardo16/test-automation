# Automation strategy

Playwright is the intended web framework. `tests/web`, `tests/api`, and `tests/helpers` are non-executing scaffolds. Diagnostics remain `CANDIDATE_FOR_MIGRATION` until approved.
