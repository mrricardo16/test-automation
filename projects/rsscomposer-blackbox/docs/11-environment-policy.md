# Environment Policy

Real Runtime values are supplied only through ignored `config/project.local.json` or environment variables. `config/project.example.json` contains placeholders and variable names only. Credentials and Runtime URLs must not be committed.
