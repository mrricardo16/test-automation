# Regression Policy

Future bug-fix regression reuses the long-lived `TC-BB-REAL-*` TestCase. Each execution gets a new RunId; equivalent TestCases are not duplicated. A TestCase moves through its document lifecycle independently from ExecutionStatus.
