# Coverage Strategy

Long-term coverage is tracked by P0, P1, and P2 priority and by Happy Path, Validation, Permission, State Transition, Boundary, Error, API, UI, Data Consistency, and Manual Boundary dimensions.

Coverage uses the Canonical Contract and Handoff stable IDs. Runtime Actual never becomes Expected. P0 gaps prevent a regression-complete claim.
