# CI isolation

The existing `test:ci` and `test:platform` target platform validation scripts; Playwright defaults to `tests/web`. This project resides under `projects/` and is not selected. No CI or GitHub Actions change is required or made.
