# Data and credentials

No credentials or Runtime URL are committed. Configuration uses `<runtime_base_url>` and environment-variable names only. Test data requires a safe documented setup and cleanup plan.
