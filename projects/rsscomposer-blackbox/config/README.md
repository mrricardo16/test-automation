# Local configuration

Copy `project.example.json` to a local ignored configuration file only after execution authorization. Replace `<runtime_base_url>` locally. Credentials are supplied only through `WEB_TEST_USERNAME` and `WEB_TEST_PASSWORD`; never store credential values in this repository.
