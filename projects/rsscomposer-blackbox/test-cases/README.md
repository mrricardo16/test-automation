# Durable TestCases

`draft/` contains imported, inactive cases. `active/` is empty until handoff integrity and execution prerequisites are approved. `retired/` retains superseded records. Every case preserves its original `TestCaseId`; Expected is traceable to the Frozen Handoff, not to a historical run.
