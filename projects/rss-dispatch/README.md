# RSS调度项目测试

本目录存放本次 `http://localhost:8223` 前端测试的全部项目产物：测试依据映射、TestCase、自动化脚本、截图/Trace/日志和执行报告。

## 本次范围

- `TC-RSS-LOGIN-001`、`TC-RSS-SM-USER-001`：登录和用户管理正式 Playwright 回归。
- `TC-RSS-SM-READ-001`：系统管理五个子模块只读基线。
- `TC-RSS-FL-AUTH-01/02/03/04/06`：按功能文档和权限流程文档执行普通账号权限链、在线变更、禁用、无会话访问和 `sa` 特殊账号核验。
- `TC-RSS-FL-AUTH-05/07`：桌面并发、许可证/系统时间注入，按计划跳过。

完整结果见 [重执行汇总](reports/重执行汇总.md)。本轮发现新增菜单“保存成功”但刷新后不在菜单树中的异常线索，并保留了独立浏览器证据。

数据 CRUD 重执行见 [CRUD重执行汇总](reports/CRUD重执行汇总.md)，使用 `sa` 仅准备和清理，正式 CRUD 使用普通管理员。

敏感凭据只通过当前执行进程环境变量传递，不写入本目录文件、截图或报告。
