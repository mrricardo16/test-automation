# Web 文档执行报告（2026-08-20）

## 执行对象

- 前端：`http://192.168.12.195:8223/`
- 账号：`sa`（密码未写入报告）
- 执行方式：仓库现有 Playwright 真实页面测试
- 依据：用户指定的《功能_02_Web管理端_系统管理(4).md》和《流程_01_权限与登录(3).md》
- TestCase 前置：使用已有 `test-cases/web/` 记录，未新增无记录的正式测试

## 结果

| 范围 | 结果 |
|---|---|
| `TC-WEB-LOGIN-001` 登录后进入 Dashboard | PASS（两次独立执行均 PASS） |
| 系统管理 28 条现有 Web TestCase | 最终 28/28 PASS |
| 初次全量执行中的环境异常 | `TC-SM-DICT-004` 首次为 ERROR，未进入业务步骤；重跑 PASS |

系统管理套件覆盖了用户、角色、角色权限、菜单、字典、外部系统配置，以及最终用户-角色关系和清理校验。测试数据使用 `CS` / `CS-20260820` 约定前缀；菜单、外部系统和测试用户已清理。系统类型字典按文档保护规则保留，删除请求被页面拒绝并由 `TC-SM-DICT-005` PASS 验证。

## 环境异常证据

首次全量执行在 `TC-SM-DICT-004` 的登录准备阶段停留在加载页，页面资源请求出现 11 次 `net::ERR_NETWORK_CHANGED`，因此分类为环境 `ERROR`，不是产品业务 `FAIL`。原始证据保留在：

`projects/test-workflow/artifacts/web/TC-SM-DICT-004/20260820-042737Z-pid5520-w0-r0/`

该用例网络恢复后单独重跑并 PASS；随后中断后的 10 条用例全部 PASS。

## 覆盖边界

本次执行完成的是仓库现有系统管理 Web 套件和 SA 登录回归，不宣称已覆盖流程文档的全部 `FL-AUTH-01` 至 `FL-AUTH-07`。以下内容仍需单独的跨浏览器/接口/主机环境测试：普通账号从零创建后登录与菜单最小权限、在线账号权限变更生效时机、禁用/删除账号对会话的影响、无效 token/Redis 登录态、并发覆盖、授权文件异常。流程文档本身也将网络中断重连列为本册边界外场景。
