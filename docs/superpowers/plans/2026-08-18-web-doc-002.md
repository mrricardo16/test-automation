# WEB-DOC-002 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Based on already executed real Web evidence, create an editable Chinese DOCX manual covering real login and only the confirmed System Management operations, with embedded clean screenshots.

**Architecture:** Build a documentation-only evidence layer under `artifacts/web-doc/system-manual/` with raw/annotated screenshots and `manifest.json`. Generate the DOCX from the manifest with Python `python-docx`, using the `compact_reference_guide` preset, real Heading styles, a static TOC, inline images, and a separate technical report.

**Tech Stack:** Playwright-compatible in-app browser for documentation capture, Python `python-docx`, bundled Python/Node runtimes, OpenXML ZIP inspection, LibreOffice render QA, PowerShell and Git checks.

## Global Constraints

- Modify only `E:\automated-testing`; keep `D:\HZ_RSS40\03_trunk\src_m_ui` read-only.
- Reuse existing TestCases and PASS evidence; do not broaden into untested System Management modules.
- Exclude FAIL, ERROR, BLOCKED, and untested operations from formal user procedures.
- Do not write the approved login password, token, cookie, or sensitive runtime values into DOCX, Markdown, JSON, filenames, or screenshots.
- Keep documentation screenshots separate from failure evidence and preserve raw screenshots before annotation.
- Use real editable text plus embedded inline images; do not make the DOCX a page-image collage.
- Verify source-tree hash before/after and require equality.

### Task 1: Evidence map and source integrity

**Files:**
- Create: `artifacts/web-doc/system-manual/manifest.json`
- Create: `projects/test-workflow/reports/web-doc-002-report.md`
- Create: `docs/superpowers/plans/2026-08-18-web-doc-002.md`

- [ ] Record initial Git state and source-tree hash.
- [ ] Read existing login TestCase, System Management execution report, existing screenshots, and source route/view labels.
- [ ] Build `ExistingTestCoverageMap` in the report and manifest, including TestCaseId, title, status, operation, evidence, and manual eligibility.

### Task 2: Documentation screenshots

**Files:**
- Create: `artifacts/web-doc/system-manual/raw/**/*.png`
- Create: `artifacts/web-doc/system-manual/annotated/**/*.png`

- [ ] Capture or safely reuse clean real-page screenshots for login and confirmed PASS procedures only.
- [ ] Use a consistent 1280x720 viewport where possible; keep screenshots free of DevTools, browser chrome, test labels, and failure toasts.
- [ ] Annotate only documentation copies with accurate numbered markers/arrows; retain raw originals.
- [ ] Populate manifest entries with chapter, feature, stepId, action, expectedResult, rawScreenshot, annotatedScreenshot, sourceTestCase, and executionStatus.

### Task 3: DOCX generator

**Files:**
- Create: `scripts/web-doc-002/generate_manual.py`
- Create: `scripts/web-doc-002/verify_manual.py`
- Create: `outputs/web-doc/系统操作手册.docx`

- [ ] Generate A4 portrait, normal margins, compact reference guide typography, cover, editable metadata, static TOC, login chapter, System Management chapter, and notes.
- [ ] Use Heading 1/2/3 styles and inline images with captions `图 N ...`; keep image aspect ratios and page width within margins.
- [ ] Generate from `manifest.json`, not from scattered screenshot paths.
- [ ] Verify package validity, `word/media/` image count, required headings, no forbidden credentials, and image-to-manifest consistency.

### Task 4: Render, review, and closeout

**Files:**
- Modify: `projects/test-workflow/reports/web-doc-002-report.md`

- [ ] Render DOCX to page PNGs with the bundled renderer and inspect every page for clipping, overlap, blank pages, image overflow, captions, and TOC presence.
- [ ] Run existing focused Web smoke baseline without changing its TestCase status.
- [ ] Recompute source hash and verify equality, `git diff --check`, and secret scan over new/modified tracked-scope content.
- [ ] Record final DOCX path, size, image count, chapters, excluded features, source integrity, Git state, and WEB-DOC-002 status.
