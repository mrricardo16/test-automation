# ScenarioSuite

- ScenarioSuiteId: `SS-`
- ModuleId / FeatureId:
- Title / Objective:
- BusinessFlow:
- IncludedTestCases:
- LifecyclePath:
- CoverageTags / Risks:
- CleanupStrategy:
- ExpectationGapIds:

ScenarioSuite 聚合 source-driven 覆盖，不包含 ExecutionStatus；每个执行结果仍绑定独立 TestCaseId。
