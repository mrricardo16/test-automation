interface:
  display_name: "Test Execution"
  short_description: "Execute black-box Web, API, or Manual tests from a handoff pack"
  default_prompt: "Use $test-execution to consume a DEV-TEST-HANDOFF pack and execute or plan black-box tests against a supplied runtime without reading product source."
